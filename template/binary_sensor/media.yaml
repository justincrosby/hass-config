- binary_sensor:
  - default_entity_id: binary_sensor.home_audio_playing
    name: Is home audio playing?
    state: '{{ is_state("media_player.home_audio", "playing") }}'
    delay_off:
      seconds: 30