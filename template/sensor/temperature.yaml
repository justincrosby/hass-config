- sensor:
  - default_entity_id: sensor.current_temp
    name: Current Temperature
    icon: mdi:thermometer
    unit_of_measurement: "°C"
    state: >
      {{ ((states("sensor.thermostat_air_temperature")|float(0) + states("sensor.bedroom_sensor_air_temperature")|float(0) + states("sensor.office_sensor_air_temperature")|float(0)) / 3) | round(1) }}
  - default_entity_id: sensor.thermostat_target_temp
    name: Thermostat Target Temperature
    icon: mdi:thermometer
    state: "{{ state_attr('climate.thermostat', 'temperature') }}"
