platform: template
sensors:
  justin_home_binary:
    value_template: >
      {% if states('sensor.justin_home_percent')|int > 10 %}
        home
      {% else %}
        not_home
      {% endif %}
    friendly_name: Justin Home
  jenni_home_binary:
    value_template: >
      {% if states('sensor.jenni_home_percent')|int > 10 %}
        home
      {% else %}
        not_home
      {% endif %}
    friendly_name: Jenni Home
  jayden_home_binary:
    value_template: >
      {% if states('sensor.jayden_home_percent')|int > 10 %}
        home
      {% else %}
        not_home
      {% endif %}
    friendly_name: Jayden Home