platform: template
sensors:
  receiver_volume:
    value_template: >
      {% if is_state("media_player.denon_avr_x1300w", "off") %}
        0
      {% else %}
        {{ (state_attr("media_player.denon_avr_x1300w", "volume_level") * 100)|int }}
      {% endif %}
    friendly_name: 'Receiver Volume'
  chromecast_volume:
    value_template: >
      {% if is_state(states("sensor.media_output"), "off") %}
        off
      {% else %}
        {{ (state_attr(states("sensor.media_output"), "volume_level") * 100)|int }}
      {% endif %}
    friendly_name: 'Chromecast Volume'
